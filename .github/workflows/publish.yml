name: Publish Maven package

on:
  push:
    branches:
      - v2
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven
          server-id: github          # must match the <id> or alt repo id
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN   # <– env var name, not the secret itself


      - name: Build and publish to GitHub Packages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mvn -B -DskipTests deploy \
          -DaltDeploymentRepository=github::default::https://maven.pkg.github.com/${{ github.repository }} \
          -DaltSnapshotDeploymentRepository=github::default::https://maven.pkg.github.com/${{ github.repository }}

